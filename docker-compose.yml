services:
  mongodb:
    image: mongo:7
    container_name: pickleball-mongodb
    ports:
      - "27017:27017"
    volumes:
      - pickleball_mongo_data:/data/db
    restart: unless-stopped

volumes:
  pickleball_mongo_data:
